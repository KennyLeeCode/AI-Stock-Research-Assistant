# =====================================================================
# Copy this file to `frontend/.env`.
#
# WARNING: Every VITE_* variable is INLINED INTO THE PUBLIC BUNDLE.
# Only put non-secret values here. API keys live in backend/.env ONLY.
# =====================================================================

VITE_API_BASE_URL=http://localhost:8000
