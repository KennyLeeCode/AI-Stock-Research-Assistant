# Development and test dependencies.
#
#   pip install -r requirements-dev.txt
#
# Pulls in the runtime requirements too, so this is the only file you need
# locally. The production container installs requirements.txt alone.

-r requirements.txt

pytest==9.1.1
pytest-asyncio==1.4.0
pytest-cov==7.1.0
# Intercepts httpx calls so the suite never touches a real market data API.
respx==0.23.1
