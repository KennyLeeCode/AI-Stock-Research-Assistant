# Runtime dependencies only. Test tooling lives in requirements-dev.txt so the
# production container does not ship pytest and its transitive dependencies.
#
# Python 3.12 or 3.13 recommended.

# --- Web framework ---
fastapi==0.140.0
uvicorn[standard]==0.51.0

# --- Data validation / settings ---
pydantic==2.13.4
pydantic-settings==2.14.2

# --- Database ---
SQLAlchemy==2.0.51

# --- HTTP client for external market-data APIs ---
httpx==0.28.1

# --- AI provider ---
# Structured outputs (output_config.format) require a recent SDK.
anthropic==0.120.0

# Optional database drivers — install only when moving off SQLite:
#   PostgreSQL: psycopg[binary]
#   MySQL:      PyMySQL
